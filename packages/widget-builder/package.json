{
  "name": "widget-builder",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "dotenv -- tsup --publicDir",
    "dev": "tsup --watch",
    "start": "dotenv -e ./.env.local node ./dist/index.js"
  },
  "dependencies": {
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "dotenv-cli": "latest",
    "tsconfig": "workspace:*",
    "tsup": "^6.7.0"
  }
}
